#include<stdio.h>
#include<stdlib.h>


int main(){

    int n , m , p , count = 0 , count1 = 0;

    scanf("%d", &n); // Num total de repetições
    scanf("%d", &m); // num de notas p/ fila
    int vetor[m];
    for(count = 0; count <= n ; count++){ //total de repetições

        for(count1 = 0; count1 <= m ; count1++){ // valor das notas
            
            vetor[count1] = m; 
        
        }

    return 0;
}